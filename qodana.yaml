version: "1.0"
linter: jetbrains/qodana-jvm-community:2025.1
profile:
  name: qodana.recommended
exclude:
  - name: All
    paths:
      - build
      - .gradle
      - app/build
      - helpers/build
      - app/src/test
      - app/src/androidTest
      - helpers/src/test
