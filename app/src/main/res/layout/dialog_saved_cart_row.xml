<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_vertical"
    android:minHeight="48dp"
    android:orientation="horizontal"
    android:paddingHorizontal="@dimen/margin2x">

    <TextView
        android:id="@+id/saved_cart_row_name"
        style="@style/TextAppearance.Material3.BodyLarge"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:ellipsize="end"
        android:maxLines="1" />

    <androidx.appcompat.widget.AppCompatImageButton
        android:id="@+id/saved_cart_row_rename"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/cart_rename"
        android:padding="@dimen/margin1x"
        android:src="@android:drawable/ic_menu_edit"
        app:tint="?attr/colorControlNormal" />

    <androidx.appcompat.widget.AppCompatImageButton
        android:id="@+id/saved_cart_row_delete"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/cart_delete_item"
        android:padding="@dimen/margin1x"
        android:src="@android:drawable/ic_menu_delete"
        app:tint="?attr/colorControlNormal" />
</LinearLayout>
