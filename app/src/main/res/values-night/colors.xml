<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- dark -->
    <color name="colorBackground">@color/blackOlive</color>
    <!-- pure black -->
    <color name="colorBackgroundPureBlack">@android:color/black</color>
    <color name="displayOverlayPureBlack">@null</color>

    <!-- Launch/splash background — pure black in night so OLED cold starts
         don't flash a lighter color first. -->
    <color name="windowBackground">@android:color/black</color>
</resources>
